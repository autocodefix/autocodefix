/**
 * AUTO CODE FIX — DB barrel export
 * All imports should use '@/lib/db/dal' directly,
 * but this barrel keeps any legacy '@/lib/db' imports working.
 */
export * from './dal'
export * from './types'
